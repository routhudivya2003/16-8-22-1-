#include <iostream>
using namespace std;

int main() 
{
    int a[4],i,count=0;
    for(i=0;i<4;i++)
    {
        scanf("%d",&a[i]);
        if(a[i]>=10)
        count++;
    }
    printf("%d",count);
	return 0;
}
